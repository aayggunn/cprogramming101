#include <stdio.h>

int main(void) {
	int x, y, sum, sub, multi, div;
	x=10;
	y=2;
	sum= x+y;
	sub= x-y;
	multi= x*y;
	div= x/y;
	printf("Sum= %d\n", sum);
	printf("Subtraction= %d\n", sub);
	printf("Multiplication= %d\n", multi);
	printf("Division= %d\n", div);
	return 0;
}
